import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  titulo:{
    fontSize: 35,
    color: 'darkblue',
    textAlign: 'center',
    marginTop: 40
  },

  tela:{
    marginTop: 40
  },
})

export {styles}